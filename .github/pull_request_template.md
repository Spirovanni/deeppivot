## Summary

<!-- What does this PR do? Link the relevant issue: Closes #deeppivot-XX -->

## Changes

- 

## Test plan

- [ ] Ran `pnpm run build` locally — passes
- [ ] Ran `pnpm run lint` locally — no errors
- [ ] Tested in browser (local dev)
- [ ] Any new env vars added to `.env.example`
- [ ] Any new DB tables have a migration generated (`pnpm run db:generate`) and applied (`pnpm run db:migrate`)

## Screenshots (if UI change)

<!-- Add before/after screenshots or a screen recording -->
